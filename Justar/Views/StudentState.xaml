<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:views="clr-namespace:Justar.Views"
             x:DataType="views:StudentState"
             x:Class="Justar.Views.StudentState">
    <ContentView.Content>
        <StackLayout Padding="10, 5, 10, 5">
            <Frame  Margin="0,10,0,0" BorderColor="#075EA6">
                <StackLayout Orientation="Horizontal" VerticalOptions="Center" Padding="0">
                    <CheckBox x:Name="checkFirst" Color="#F2413A" CheckedChanged="CheckedChanged"/>
                    <CheckBox x:Name="checkSeccond" Color="#F2413A" CheckedChanged="CheckedChanged"/>
                    <CheckBox x:Name="checkThird" Color="#F2413A" CheckedChanged="CheckedChanged"/>
                    <CheckBox x:Name="checkFourth" Color="#F2413A" CheckedChanged="CheckedChanged"/>
                    <Label x:Name="fio" VerticalOptions="Center" />
                    <Button Text="Редактировать" Clicked="EditClicked"/>
                </StackLayout>
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer NumberOfTapsRequired="2" Tapped="DoubleClick"/>
                </Frame.GestureRecognizers>
            </Frame>
            </StackLayout>
    </ContentView.Content>
</ContentView>